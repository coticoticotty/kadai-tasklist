<h1>id =
<%= @tasklist.id %>
のタスク詳細ページ</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td>
<%= @tasklist.id %>
</td>
  </tr>
  <tr>
    <th>ステータス</th>
    <td>
<%= @tasklist.status %>
</td>
</tr>
<tr>
  <th>タスク</th>
  <td>
  <%= @tasklist.content %>
  </td>
  </tr>
</table>

<%= link_to "このメッセージを編集する", edit_tasklist_path(@tasklist), class: "btn btn-default" %>
<%= link_to "このメッセージを削除する", @tasklist, method: :delete, data: { confirm: "本当に削除してよろしいですか？"}, class: "btn btn-danger" %>
<br><%= link_to "一覧に戻る", tasklists_path %>